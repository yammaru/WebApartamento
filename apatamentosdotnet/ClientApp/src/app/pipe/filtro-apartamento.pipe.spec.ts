import { FiltroApartamentoPipe } from './filtro-apartamento.pipe';

describe('FiltroApartamentoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroApartamentoPipe();
    expect(pipe).toBeTruthy();
  });
});
