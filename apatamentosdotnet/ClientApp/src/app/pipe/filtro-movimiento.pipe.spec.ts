import { FiltroMovimientoPipe } from './filtro-movimiento.pipe';

describe('FiltroMovimientoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroMovimientoPipe();
    expect(pipe).toBeTruthy();
  });
});
